/**
 * Created by zenit1 on 23/03/2017.
 */

function addDays(date, days) {
	let result = new Date(date || new Date());
	result.setDate(result.getDate() + days);
	return result;
}

console.log(addDays(null,-3));
